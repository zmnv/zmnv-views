const ViewsFooter = require('./footer');
const ViewsHeader = require('./header');
const Template = require('./template');
const ViewsPolls = require('./polls');

module.exports = {
    ViewsFooter,
    ViewsHeader,
    Template,
    ViewsPolls
}
